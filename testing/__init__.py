"""Scaffolding shared by the test suites of every app.

Not an installed application and not imported by any production module: it exists
so that "make a company in a state" is written once instead of once per app.
"""
